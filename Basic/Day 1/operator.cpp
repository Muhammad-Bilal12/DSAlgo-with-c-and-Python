#include <iostream>
using namespace std;

int main()
{

    // Operators

    // Arithmetic Operator

    int num1 = 10;
    int num2 = 20;

    // cout << "num1 + num2 = " << num1 + num2 << endl;
    // cout << "num1 - num2 = " << num1 - num2 << endl;
    // cout << "num1 * num2 = " << num1 * num2 << endl;
    // cout << "num2 / num1 = " << num1 / num2 << endl;
    // cout << "num2 % num1 = " << num1 % num2 << endl;

    // Increament Decrement

    // int a = 5;
    // cout<<a++<<endl;
    //    cout<<a;

    // a++ => a = a+1

    // Comparison Operator

    /**
     * equal to ==
     * greater than >
     * less than <<
     * greater than equals to =>
     * less than equals to <=
     * Not equals to !=
     */

    cout << "1 for true and 0 for false\n";
    cout << (20 == 20) << endl;
    cout << (20 == 10) << endl;
    cout << (20 > 10) << endl;
    cout << (20 < 10) << endl;
    cout << (20 <= 30) << endl;
    cout << (20 >= 20) << endl;
    cout << (20 != 20) << endl;


// Logical Operators
/*
* AND &&
* OR ||
* NOT ! 
*/

// We check this operator when learning if else statement


    return 0;
}